services:
  api:
    build: .
    container_name: payment_receipts_api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    environment:
      - DATABASE_PATH=/app/data/receipts.db
    env_file:
      - .env
    restart: unless-stopped
